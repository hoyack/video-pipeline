---
phase: 16-production-bible-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/api/client.ts
  - frontend/src/App.tsx
  - frontend/src/components/Layout.tsx
  - frontend/src/components/ProductionBibleLibrary.tsx
  - frontend/src/components/ProductionBibleCreator.tsx
  - frontend/src/components/ProductionBibleCard.tsx
  - frontend/src/components/ProductionBibleSelector.tsx
  - frontend/src/components/SceneDetail.tsx
autonomous: true
requirements:
  - PBIB-04
  - PBIB-05
  - PBIB-06

must_haves:
  truths:
    - "Frontend uses Production Bible terminology everywhere"
    - "Routes updated to /production-bibles/* paths"
    - "API client calls /api/production-bibles/* endpoints"
    - "Nav bar shows Production Bibles instead of Manifests"
    - "Production Bible detail view has three department tabs: Casting, Art Department, Sound"
    - "Assets are sorted into correct department tabs by asset_type"
  artifacts:
    - path: "frontend/src/components/ProductionBibleLibrary.tsx"
      provides: "Renamed library component with Production Bible terminology"
      contains: "Production Bible Library"
    - path: "frontend/src/components/ProductionBibleCreator.tsx"
      provides: "Renamed creator component with department tabs"
      contains: "ProductionBibleCreator"
    - path: "frontend/src/components/ProductionBibleCard.tsx"
      provides: "Renamed card component"
      contains: "ProductionBibleCard"
    - path: "frontend/src/components/ProductionBibleSelector.tsx"
      provides: "Renamed selector component"
      contains: "ProductionBibleSelector"
    - path: "frontend/src/api/types.ts"
      provides: "Renamed TypeScript types with production_bible_id"
      contains: "production_bible_id"
    - path: "frontend/src/api/client.ts"
      provides: "API client using /api/production-bibles/ paths"
      contains: "/api/production-bibles"
  key_links:
    - from: "frontend/src/api/client.ts"
      to: "backend/vidpipe/api/routes.py"
      via: "fetch calls to /api/production-bibles/*"
      pattern: "/api/production-bibles"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/ProductionBibleLibrary.tsx"
      via: "route rendering"
      pattern: "ProductionBibleLibrary"
    - from: "frontend/src/components/Layout.tsx"
      to: "frontend/src/App.tsx"
      via: "nav link to /production-bibles"
      pattern: "/production-bibles"
---

<objective>
Rename all frontend Manifest references to Production Bible: component files, TypeScript types, API client functions, routes, and navigation. Add department tab structure to the Production Bible detail/creator view.

Purpose: Issue #7 frontend — completes the user-facing rename from "Manifest" to "Production Bible" and adds the department tab navigation (Casting, Art Department, Sound) that organizes assets by type.

Output: Four renamed component files, updated types/client, routes at /production-bibles/*, department tabs in creator view.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-production-bible-foundation/16-01-SUMMARY.md
@frontend/src/api/types.ts
@frontend/src/api/client.ts
@frontend/src/App.tsx
@frontend/src/components/Layout.tsx
@frontend/src/components/ManifestLibrary.tsx
@frontend/src/components/ManifestCard.tsx
@frontend/src/components/ManifestSelector.tsx
@frontend/src/components/ManifestCreator.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename TypeScript types, API client functions, and component files</name>
  <files>
    frontend/src/api/types.ts
    frontend/src/api/client.ts
    frontend/src/components/ProductionBibleLibrary.tsx
    frontend/src/components/ProductionBibleCreator.tsx
    frontend/src/components/ProductionBibleCard.tsx
    frontend/src/components/ProductionBibleSelector.tsx
  </files>
  <action>
**In `frontend/src/api/types.ts`:**

1. Rename types:
   - `ManifestListItem` -> `ProductionBibleListItem` — rename field `manifest_id` to `production_bible_id`, `parent_manifest_id` to `parent_production_bible_id`
   - `ManifestDetail` -> `ProductionBibleDetail` — rename field `manifest_id` to `production_bible_id`, `parent_manifest_id` to `parent_production_bible_id`
   - `CreateManifestRequest` -> `CreateProductionBibleRequest`
   - `UpdateManifestRequest` -> `UpdateProductionBibleRequest`
   - `AssetResponse` — rename field `manifest_id` to `production_bible_id`
   - `SceneDetail` — rename field `manifest_id` to `production_bible_id`
   - `GenerateRequest` — rename field `manifest_id` to `production_bible_id`
   - `EditSceneRequest` — rename field `manifest_id` to `production_bible_id`
   - `CreateDraftSceneRequest` — rename field `manifest_id` to `production_bible_id`
   - `CreateAssetRequest` — keep as-is (assets are still called assets)
   - `UpdateAssetRequest` — keep as-is
   - Keep `ProcessingProgress` as-is (generic type)

2. Add backward-compat type aliases at the bottom:
```typescript
/** @deprecated Use ProductionBibleListItem */
export type ManifestListItem = ProductionBibleListItem;
/** @deprecated Use ProductionBibleDetail */
export type ManifestDetail = ProductionBibleDetail;
/** @deprecated Use CreateProductionBibleRequest */
export type CreateManifestRequest = CreateProductionBibleRequest;
/** @deprecated Use UpdateProductionBibleRequest */
export type UpdateManifestRequest = UpdateProductionBibleRequest;
```

**In `frontend/src/api/client.ts`:**

3. Update all import type references to use new type names.

4. Rename API client functions and update URL paths:
   - `listManifests` -> `listProductionBibles` — URL: `/api/production-bibles`
   - `createManifest` -> `createProductionBible` — URL: `/api/production-bibles`
   - `importSceneToManifest` -> `importSceneToProductionBible` — URL: `/api/production-bibles/from-scene`
   - `getManifestDetail` -> `getProductionBibleDetail` — URL: `/api/production-bibles/${id}`
   - `updateManifest` -> `updateProductionBible` — URL: `/api/production-bibles/${id}`
   - `deleteManifest` -> `deleteProductionBible` — URL: `/api/production-bibles/${id}` (return type field: `production_bible_id`)
   - `duplicateManifest` -> `duplicateProductionBible` — URL: `/api/production-bibles/${id}/duplicate`
   - `createAsset` — URL: `/api/production-bibles/${id}/assets` (param rename: `productionBibleId`)
   - `processManifest` -> `processProductionBible` — URL: `/api/production-bibles/${id}/process`
   - `getProcessingProgress` — URL: `/api/production-bibles/${id}/progress`
   - `uploadVideoForManifest` -> `uploadVideoForProductionBible` — URL: `/api/production-bibles/${id}/upload-video`
   - `getExtractionProgress` — URL: `/api/production-bibles/${id}/extraction-progress`
   - `fetchManifestAssets` -> `fetchProductionBibleAssets` — URL: `/api/production-bibles/${id}`

5. Add deprecated aliases for all renamed functions:
```typescript
/** @deprecated Use listProductionBibles */
export const listManifests = listProductionBibles;
// ... etc
```

**Create `frontend/src/components/ProductionBibleLibrary.tsx`:**

6. Copy content from ManifestLibrary.tsx. Rename:
   - Component name: `ProductionBibleLibrary`
   - All "Manifest" text strings to "Production Bible"
   - Import references to use new API client function names
   - State variable `manifests` -> `productionBibles`
   - Header: "Production Bible Library"
   - Button: "+ New Production Bible"
   - Empty state text: "No production bibles yet."

**Create `frontend/src/components/ProductionBibleCard.tsx`:**

7. Copy content from ManifestCard.tsx. Rename:
   - Component name: `ProductionBibleCard`
   - Props type: `ProductionBibleCardProps`
   - Use `production_bible_id` instead of `manifest_id`
   - Copy ID tooltip: "Copy production bible ID"
   - Props interface uses `ProductionBibleListItem` type

**Create `frontend/src/components/ProductionBibleSelector.tsx`:**

8. Copy content from ManifestSelector.tsx. Rename:
   - Component name: `ProductionBibleSelector`
   - Props type: `ProductionBibleSelectorProps`
   - Use new API client functions
   - Use `ProductionBibleDetail`, `ProductionBibleListItem` types
   - Button text: "Select a production bible to attach reference images"
   - Prop names: `selectedProductionBibleId`, `onProductionBibleSelect`

**Create `frontend/src/components/ProductionBibleCreator.tsx`:**

9. Copy content from ManifestCreator.tsx. Rename:
   - Component name: `ProductionBibleCreator`
   - Props type: `ProductionBibleCreatorProps`
   - Use new API client functions
   - Use `ProductionBibleDetail` type
   - All user-facing strings: "Manifest" -> "Production Bible"
   - Prop: `productionBibleId` instead of `manifestId`

Do NOT delete the old Manifest*.tsx files yet — they can be removed in a follow-up cleanup. The new files are the canonical ones.
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/video-pipeline/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check that TypeScript compiles without errors after all renames</manual>
  </verify>
  <done>All TypeScript types renamed with production_bible_id fields. API client functions renamed with /api/production-bibles/ URLs. Four new component files created with Production Bible naming. Backward-compat aliases exist for gradual migration.</done>
</task>

<task type="auto">
  <name>Task 2: Update routing, navigation, and add department tabs</name>
  <files>
    frontend/src/App.tsx
    frontend/src/components/Layout.tsx
    frontend/src/components/ProductionBibleCreator.tsx
    frontend/src/components/SceneDetail.tsx
  </files>
  <action>
**In `frontend/src/App.tsx`:**

1. Replace ManifestLibrary/ManifestCreator imports with ProductionBibleLibrary/ProductionBibleCreator.
2. Update routes:
   - `/manifests/new` -> `/production-bibles/new` (render `ProductionBibleCreator`)
   - `/manifests/:id/edit` -> `/production-bibles/:id/edit` (render `ProductionBibleCreator`)
   - `/manifests` -> `/production-bibles` (render `ProductionBibleLibrary`)
3. Update navigate calls: all `/manifests` -> `/production-bibles`
4. Update onViewManifest callback in SceneDetail: navigate to `/production-bibles/${id}/edit`

**In `frontend/src/components/Layout.tsx`:**

5. Update NAV_ITEMS:
   - Change `{ href: "/manifests", label: "Manifests", match: ... }` to `{ href: "/production-bibles", label: "Production Bibles", match: (path) => path.startsWith("/production-bibles") }`

**In `frontend/src/components/ProductionBibleCreator.tsx`:**

6. Add department tab structure to the asset editing view. The tabs organize existing assets by type.

Define department tab configuration:
```typescript
const DEPARTMENT_TABS = [
  {
    id: "casting",
    label: "Casting",
    assetTypes: ["CHARACTER"],
  },
  {
    id: "art",
    label: "Art Department",
    assetTypes: ["ENVIRONMENT", "PROP", "OBJECT", "VEHICLE", "STYLE"],
  },
  {
    id: "sound",
    label: "Sound",
    assetTypes: [],  // placeholder
  },
];
```

7. Add tab state: `const [activeTab, setActiveTab] = useState("casting")`.

8. Render pill-style tab navigation above the assets grid (only shown when in edit mode with existing assets, i.e., stage 3):
```tsx
<div className="mb-4 flex gap-2">
  {DEPARTMENT_TABS.map((tab) => {
    const count = tab.assetTypes.length > 0
      ? assets.filter((a) => tab.assetTypes.includes(a.asset_type)).length
      : 0;
    return (
      <button
        key={tab.id}
        onClick={() => setActiveTab(tab.id)}
        className={`rounded-full px-4 py-1.5 text-sm font-medium transition-colors ${
          activeTab === tab.id
            ? "bg-blue-600 text-white"
            : "bg-gray-800 text-gray-400 hover:text-gray-200"
        }`}
      >
        {tab.label}
        {count > 0 && (
          <span className="ml-1.5 rounded-full bg-gray-700 px-1.5 py-0.5 text-xs">
            {count}
          </span>
        )}
      </button>
    );
  })}
</div>
```

9. Filter the displayed assets based on active tab:
```typescript
const activeTabConfig = DEPARTMENT_TABS.find((t) => t.id === activeTab);
const filteredAssets = activeTab === "sound"
  ? []  // Sound tab is placeholder
  : assets.filter((a) => activeTabConfig?.assetTypes.includes(a.asset_type));
```

10. For the Sound tab, show a placeholder message:
```tsx
{activeTab === "sound" && (
  <div className="flex min-h-[200px] items-center justify-center rounded-lg border border-dashed border-gray-700">
    <p className="text-sm text-gray-500">Audio direction coming soon</p>
  </div>
)}
```

11. Render the assets grid using `filteredAssets` instead of `assets` (only for the asset display section, not uploads/processing).

**In `frontend/src/components/SceneDetail.tsx`:**

12. Update any references that navigate to manifests: change `onViewManifest` callback path from `/manifests/` to `/production-bibles/`.
13. Update any displayed text from "Manifest" to "Production Bible" (e.g., "View Manifest" -> "View Production Bible").
14. Update any `manifest_id` field references to `production_bible_id`.

Also search for and update any other components that reference ManifestSelector (likely GenerateForm.tsx or VideoGenEditor.tsx) — import ProductionBibleSelector instead and update prop names.
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/video-pipeline/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Run dev server and verify: nav shows "Production Bibles", routes work at /production-bibles/*, department tabs visible in creator view</manual>
  </verify>
  <done>Frontend routes updated to /production-bibles/*. Nav shows "Production Bibles". Department tabs (Casting, Art Department, Sound) appear in ProductionBibleCreator with assets filtered by type. Sound tab shows "Coming soon" placeholder.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Nav bar shows "Production Bibles" link pointing to /production-bibles
3. /production-bibles route renders ProductionBibleLibrary
4. /production-bibles/new renders ProductionBibleCreator
5. /production-bibles/:id/edit renders ProductionBibleCreator with department tabs
6. Department tabs: Casting shows CHARACTER assets, Art Dept shows ENV/PROP/OBJECT/VEHICLE/STYLE, Sound shows placeholder
7. API client uses /api/production-bibles/ URLs
</verification>

<success_criteria>
- All frontend "Manifest" terminology replaced with "Production Bible"
- Routes at /production-bibles/* functional
- API client calls /api/production-bibles/ endpoints
- Department tabs visible in Production Bible creator/editor
- Casting tab filters CHARACTER assets
- Art Department tab filters ENVIRONMENT, PROP, OBJECT, VEHICLE, STYLE assets
- Sound tab shows "Coming soon" placeholder
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-production-bible-foundation/16-03-SUMMARY.md`
</output>
