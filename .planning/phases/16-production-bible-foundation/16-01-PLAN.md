---
phase: 16-production-bible-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/vidpipe/db/models.py
  - backend/vidpipe/db/__init__.py
  - backend/vidpipe/api/routes.py
  - backend/vidpipe/services/manifest_service.py
  - backend/vidpipe/services/manifesting_engine.py
  - backend/vidpipe/services/entity_extraction.py
  - backend/vidpipe/workers/processing_tasks.py
  - backend/vidpipe/orchestrator/pipeline.py
  - backend/vidpipe/pipeline/storyboard.py
autonomous: true
requirements:
  - PBIB-01
  - PBIB-02
  - PBIB-03

must_haves:
  truths:
    - "production_bibles table exists in the database with all data from manifests"
    - "All FK columns referencing manifests use production_bible_id naming"
    - "All API endpoints respond at /api/production-bibles/* paths"
    - "Legacy /api/manifests/* paths return 301 redirects to /api/production-bibles/*"
    - "Internal Python code references ProductionBible model instead of Manifest"
  artifacts:
    - path: "backend/vidpipe/db/models.py"
      provides: "ProductionBible model (renamed from Manifest)"
      contains: "class ProductionBible"
    - path: "backend/vidpipe/db/__init__.py"
      provides: "Migration renaming manifests table to production_bibles"
      contains: "production_bibles"
    - path: "backend/vidpipe/api/routes.py"
      provides: "Renamed API endpoints and 301 redirects"
      contains: "/production-bibles"
    - path: "backend/vidpipe/services/manifest_service.py"
      provides: "Service layer updated to use ProductionBible model"
      contains: "ProductionBible"
  key_links:
    - from: "backend/vidpipe/api/routes.py"
      to: "backend/vidpipe/db/models.py"
      via: "ProductionBible model import"
      pattern: "from vidpipe.db.models import.*ProductionBible"
    - from: "backend/vidpipe/api/routes.py"
      to: "backend/vidpipe/services/manifest_service.py"
      via: "service function calls"
      pattern: "manifest_service\\."
    - from: "backend/vidpipe/db/__init__.py"
      to: "backend/vidpipe/db/models.py"
      via: "migration references new table name"
      pattern: "production_bibles"
---

<objective>
Rename the Manifest concept to Production Bible across the entire backend: database model, table name, FK columns, Pydantic schemas, API endpoints, service layer, and all internal references. Add 301 redirects from legacy /api/manifests/* paths.

Purpose: Issue #7 backend — establishes the Production Bible naming convention that the frontend (Plan 03) will adopt. The rename must be backwards-compatible during the transition via 301 redirects.

Output: All backend code uses "ProductionBible" / "production_bible" naming. API responds at /api/production-bibles/*. Legacy paths redirect.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/vidpipe/db/models.py
@backend/vidpipe/db/__init__.py
@backend/vidpipe/api/routes.py
@backend/vidpipe/services/manifest_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename database model and add migration</name>
  <files>
    backend/vidpipe/db/models.py
    backend/vidpipe/db/__init__.py
  </files>
  <action>
In `backend/vidpipe/db/models.py`:

1. Rename `class Manifest` to `class ProductionBible` with `__tablename__ = "production_bibles"`.
2. Rename `parent_manifest_id` column to `parent_production_bible_id` with updated ForeignKey: `ForeignKey("production_bibles.id")`.
3. In `class Asset`, rename `manifest_id` to `production_bible_id` with updated ForeignKey: `ForeignKey("production_bibles.id")`.
4. In `class ManifestSnapshot`, rename `manifest_id` to `production_bible_id` with updated ForeignKey: `ForeignKey("production_bibles.id")`.
5. In `class Scene`, rename `manifest_id` to `production_bible_id` with updated ForeignKey: `ForeignKey("production_bibles.id")`, and rename `manifest_version` to `production_bible_version`.
6. Add a backwards-compatibility alias at module level: `Manifest = ProductionBible` (temporary, helps avoid import breakage during transition).

In `backend/vidpipe/db/__init__.py`:

7. Add table rename migration. For SQLite: use `ALTER TABLE manifests RENAME TO production_bibles`. For PostgreSQL: same syntax works. This must run BEFORE create_all() since create_all() would try to create a new `production_bibles` table.
8. Add column rename migrations for FK columns. SQLite does NOT support ALTER TABLE RENAME COLUMN until 3.25.0 — check the version. For SQLite 3.25+, use `ALTER TABLE assets RENAME COLUMN manifest_id TO production_bible_id`. For older SQLite or PostgreSQL, use `ALTER TABLE assets RENAME COLUMN manifest_id TO production_bible_id` (PG supports this natively).
9. Add the same column renames for: `manifest_snapshots.manifest_id` -> `production_bible_id`, `scenes.manifest_id` -> `production_bible_id`, `scenes.manifest_version` -> `production_bible_version`, `production_bibles.parent_manifest_id` -> `parent_production_bible_id`.
10. Update the import line in `__init__.py` to import `ProductionBible` instead of (or in addition to) `Manifest`.

IMPORTANT: The rename migrations must be IDEMPOTENT — wrap in try/except like existing migrations. Place table rename migration BEFORE column renames. Run table rename BEFORE `create_all()` so SQLAlchemy finds the existing table under its new name.

Do NOT rename `ShotManifest`, `ShotAudioManifest`, or `manifest_tag` on Asset — these are different domain concepts (per-shot manifest, not the production bible).
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/video-pipeline && python -c "from vidpipe.db.models import ProductionBible, Manifest, Asset, ManifestSnapshot, Scene; assert ProductionBible.__tablename__ == 'production_bibles'; assert hasattr(Asset, 'production_bible_id'); assert hasattr(Scene, 'production_bible_id'); print('OK')"</automated>
    <manual>Verify the model class and FK column renames compiled correctly</manual>
  </verify>
  <done>ProductionBible model exists with correct __tablename__. All FK columns renamed. Migration code added for table and column renames. Backward-compatible Manifest alias exists.</done>
</task>

<task type="auto">
  <name>Task 2: Rename API endpoints, Pydantic schemas, service layer, and all internal references</name>
  <files>
    backend/vidpipe/api/routes.py
    backend/vidpipe/services/manifest_service.py
    backend/vidpipe/services/manifesting_engine.py
    backend/vidpipe/services/entity_extraction.py
    backend/vidpipe/workers/processing_tasks.py
    backend/vidpipe/orchestrator/pipeline.py
    backend/vidpipe/pipeline/storyboard.py
  </files>
  <action>
**In `backend/vidpipe/api/routes.py`:**

1. Rename Pydantic schemas:
   - `CreateManifestRequest` -> `CreateProductionBibleRequest`
   - `UpdateManifestRequest` -> `UpdateProductionBibleRequest`
   - `CreateManifestFromSceneRequest` -> `CreateProductionBibleFromSceneRequest`
   - `ManifestListItem` -> `ProductionBibleListItem`
   - `ManifestDetailResponse` -> `ProductionBibleDetailResponse`
   - In all these schemas, rename field `manifest_id` -> `production_bible_id` and `parent_manifest_id` -> `parent_production_bible_id`.

2. Rename all route paths from `/manifests` to `/production-bibles`:
   - `POST /manifests` -> `POST /production-bibles`
   - `POST /manifests/from-scene` -> `POST /production-bibles/from-scene`
   - `GET /manifests` -> `GET /production-bibles`
   - `GET /manifests/{manifest_id}` -> `GET /production-bibles/{production_bible_id}`
   - `PUT /manifests/{manifest_id}` -> `PUT /production-bibles/{production_bible_id}`
   - `DELETE /manifests/{manifest_id}` -> `DELETE /production-bibles/{production_bible_id}`
   - `POST /manifests/{manifest_id}/duplicate` -> `POST /production-bibles/{production_bible_id}/duplicate`
   - `POST /manifests/{manifest_id}/assets` -> `POST /production-bibles/{production_bible_id}/assets`
   - `GET /manifests/{manifest_id}/assets` -> `GET /production-bibles/{production_bible_id}/assets`
   - `POST /manifests/{manifest_id}/process` -> `POST /production-bibles/{production_bible_id}/process`
   - `GET /manifests/{manifest_id}/progress` -> `GET /production-bibles/{production_bible_id}/progress`
   - `POST /manifests/{manifest_id}/upload-video` -> `POST /production-bibles/{production_bible_id}/upload-video`
   - `GET /manifests/{manifest_id}/extraction-progress` -> `GET /production-bibles/{production_bible_id}/extraction-progress`

3. Update all function parameter names from `manifest_id` to `production_bible_id` in these route handler functions.

4. Update model imports: change `Manifest` to `ProductionBible` in the import from `vidpipe.db.models`.

5. Update all internal references: `Manifest.id`, `Manifest.name`, etc. to `ProductionBible.id`, `ProductionBible.name`, etc.

6. Add 301 redirect routes for ALL legacy `/manifests/*` paths. Use a redirect helper function:
```python
from fastapi.responses import RedirectResponse

def _legacy_manifest_redirect(request):
    """Redirect legacy /api/manifests/* to /api/production-bibles/*."""
    new_path = request.url.path.replace("/manifests", "/production-bibles", 1)
    if request.url.query:
        new_path += f"?{request.url.query}"
    return RedirectResponse(url=new_path, status_code=301)
```
Add catch-all redirect routes at the END of the router:
```python
@router.api_route("/manifests/{path:path}", methods=["GET", "POST", "PUT", "DELETE", "PATCH"])
async def legacy_manifest_redirect(request: Request, path: str):
    return _legacy_manifest_redirect(request)

@router.api_route("/manifests", methods=["GET", "POST"])
async def legacy_manifest_list_redirect(request: Request):
    return _legacy_manifest_redirect(request)
```

7. Update `manifest_id` field references in other Pydantic schemas (e.g., `GenerateRequest`, `AssetResponse`, `SceneDetailResponse`) to `production_bible_id`.

**In `backend/vidpipe/services/manifest_service.py`:**

8. Update import: `from vidpipe.db.models import Asset, Keyframe, Manifest, ManifestSnapshot, Scene, Shot` -> replace `Manifest` with `ProductionBible`.
9. Update all internal references from `Manifest` to `ProductionBible` (e.g., `Manifest()` -> `ProductionBible()`, `Manifest.id` -> `ProductionBible.id`).
10. Update `.manifest_id` attribute accesses to `.production_bible_id` on Asset, Scene, ManifestSnapshot objects.

**In `backend/vidpipe/services/manifesting_engine.py`:**

11. Update import: `from vidpipe.db.models import Asset, Manifest` -> `from vidpipe.db.models import Asset, ProductionBible`.
12. Update all `Manifest` references to `ProductionBible`.
13. Update `.manifest_id` attribute accesses to `.production_bible_id`.

**In `backend/vidpipe/services/entity_extraction.py`:**

14. Update import and references: `Manifest` -> `ProductionBible`, `.manifest_id` -> `.production_bible_id`.

**In `backend/vidpipe/workers/processing_tasks.py`:**

15. Update import and references: `Manifest` -> `ProductionBible`, `.manifest_id` -> `.production_bible_id`.

**In `backend/vidpipe/orchestrator/pipeline.py`:**

16. Update any `manifest_id` references on Scene objects to `production_bible_id`.
17. The `manifest_service` import and function calls stay the same (function names don't need renaming, just the model references inside them).

**In `backend/vidpipe/pipeline/storyboard.py`:**

18. Update `manifest_service.load_manifest_assets` call — the function name stays but update any `manifest_id` parameter references to `production_bible_id`.

Do NOT rename: `ShotManifest`, `ShotAudioManifest`, `manifest_tag` on Assets, `shot_manifests` table, `shot_audio_manifests` table. These are per-shot concepts, not the production bible.
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/video-pipeline && python -c "from vidpipe.api.routes import router; routes = [r.path for r in router.routes if hasattr(r, 'path')]; pb_routes = [r for r in routes if '/production-bibles' in r]; legacy = [r for r in routes if '/manifests' in r and '/production-bibles' not in r]; print(f'production-bible routes: {len(pb_routes)}'); print(f'legacy redirect routes: {len(legacy)}'); assert len(pb_routes) >= 14, f'Expected 14+ production-bible routes, got {len(pb_routes)}'; assert len(legacy) >= 1, 'Expected legacy redirect routes'; print('OK')"</automated>
    <manual>Verify API imports and route renaming completed without syntax errors</manual>
  </verify>
  <done>All API endpoints respond at /api/production-bibles/*. Legacy /api/manifests/* paths have 301 redirects. All Pydantic schemas renamed. All internal Python code uses ProductionBible model. ShotManifest/ShotAudioManifest/manifest_tag unchanged.</done>
</task>

</tasks>

<verification>
1. `python -c "from vidpipe.db.models import ProductionBible; print(ProductionBible.__tablename__)"` prints "production_bibles"
2. `python -c "from vidpipe.api.routes import router"` imports without error
3. All 14+ manifest API routes now respond at /production-bibles/* paths
4. Legacy /api/manifests/* paths have 301 redirect handlers
5. ShotManifest and ShotAudioManifest models are unchanged
</verification>

<success_criteria>
- ProductionBible model with production_bibles table name exists
- All FK columns renamed (production_bible_id)
- Database migration renames table and columns idempotently
- 14+ API routes at /api/production-bibles/* paths
- 301 redirects for legacy /api/manifests/* paths
- All internal Python references updated
- No references to old Manifest model (except ShotManifest/ShotAudioManifest/backward-compat alias)
</success_criteria>

<output>
After completion, create `.planning/phases/16-production-bible-foundation/16-01-SUMMARY.md`
</output>
