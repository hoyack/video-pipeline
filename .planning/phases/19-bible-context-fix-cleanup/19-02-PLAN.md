---
phase: 19-bible-context-fix-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ShotCard.tsx
  - frontend/src/components/EditForkPanel.tsx
  - frontend/src/components/ManifestLibrary.tsx
  - frontend/src/components/ManifestCreator.tsx
  - frontend/src/components/ManifestCard.tsx
  - frontend/src/components/ManifestSelector.tsx
autonomous: true
requirements:
  - SCRN-10

must_haves:
  truths:
    - "No user-facing 'manifest' strings remain in ShotCard.tsx or EditForkPanel.tsx"
    - "Orphan Manifest component files are deleted from the codebase"
    - "EditForkPanel imports fetchProductionBibleAssets (not fetchManifestAssets)"
    - "Frontend builds without errors after cleanup"
  artifacts:
    - path: "frontend/src/components/ShotCard.tsx"
      provides: "Updated user-facing strings (manifest → Production Bible / Bible)"
      contains: "Production Bible"
    - path: "frontend/src/components/EditForkPanel.tsx"
      provides: "Updated import and user-facing string"
      contains: "fetchProductionBibleAssets"
  key_links:
    - from: "frontend/src/components/EditForkPanel.tsx"
      to: "frontend/src/api/client.ts"
      via: "import fetchProductionBibleAssets"
      pattern: "fetchProductionBibleAssets"
---

<objective>
Remove dead manifest frontend code and fix remaining user-facing "manifest" strings to use "Production Bible" terminology.

Purpose: Phase 16 renamed Manifest to Production Bible but left behind four orphan component files (1,885 lines) and user-facing "manifest" strings in ShotCard.tsx and EditForkPanel.tsx. This cleanup eliminates dead code and ensures consistent terminology.

Output: Four orphan files deleted, two files updated with correct terminology, clean frontend build.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-bible-context-fix-cleanup/19-RESEARCH.md
@frontend/src/components/ShotCard.tsx
@frontend/src/components/EditForkPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix user-facing manifest strings in ShotCard.tsx and EditForkPanel.tsx</name>
  <files>
    frontend/src/components/ShotCard.tsx
    frontend/src/components/EditForkPanel.tsx
  </files>
  <action>
1. **frontend/src/components/ShotCard.tsx** — Fix 2 user-facing "manifest" strings:
   - Line 261: Change `title={canNavigate ? \`${ref.name} — Click to view manifest\` : ref.name}` to `title={canNavigate ? \`${ref.name} — Click to view Production Bible\` : ref.name}`
   - Line 373: Change `<span>Manifest</span>` (label for manifest_adherence_score) to `<span>Bible</span>`
   - Do NOT rename `manifest_tag` or `manifest_adherence_score` data field references (lines 270, 276, 371, 625) — those are API field names that match the DB column names. Renaming those would require a DB migration which is out of scope.

2. **frontend/src/components/EditForkPanel.tsx** — Fix 2 manifest references:
   - Line 3: Change import `fetchManifestAssets` to `fetchProductionBibleAssets`:
     ```typescript
     import { forkScene, fetchProductionBibleAssets, getEnabledModels } from "../api/client.ts";
     ```
   - Line 106: Change the call from `fetchManifestAssets(detail.production_bible_id)` to `fetchProductionBibleAssets(detail.production_bible_id)` (same function, just using the canonical name)
   - Line 599: Change `"No assets in manifest"` to `"No assets in Production Bible"`
   - Do NOT rename `manifest_tag` data field references (lines 625, 632) — those are API field names.
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/video-pipeline/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Search for user-facing "manifest" strings in ShotCard.tsx and EditForkPanel.tsx — should find none</manual>
  </verify>
  <done>ShotCard.tsx shows "Production Bible" and "Bible" instead of "manifest"/"Manifest"; EditForkPanel.tsx imports fetchProductionBibleAssets and shows "No assets in Production Bible"</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphan Manifest component files</name>
  <files>
    frontend/src/components/ManifestLibrary.tsx
    frontend/src/components/ManifestCreator.tsx
    frontend/src/components/ManifestCard.tsx
    frontend/src/components/ManifestSelector.tsx
  </files>
  <action>
Delete these four orphan files that were superseded by ProductionBible* equivalents in Phase 16:

```bash
rm frontend/src/components/ManifestLibrary.tsx
rm frontend/src/components/ManifestCreator.tsx
rm frontend/src/components/ManifestCard.tsx
rm frontend/src/components/ManifestSelector.tsx
```

These files have ZERO external imports (verified via grep — only ManifestLibrary.tsx imports ManifestCard.tsx, which is self-referential within the orphan set). No component, route, or page file imports any of these four components.

After deletion, verify no remaining imports reference them:
```bash
grep -r "ManifestLibrary\|ManifestCreator\|ManifestCard\|ManifestSelector" frontend/src/ --include="*.tsx" --include="*.ts"
```
This should return zero results.

Also check if `types.ts` has backward-compat aliases like `ManifestListItem` that are now unused:
- If `ManifestListItem` or other `Manifest*` type aliases exist in `frontend/src/api/types.ts` and are only referenced by the deleted files, remove those aliases too.
- If they are still referenced by other files (e.g., client.ts), leave them — they serve as backward-compat aliases.
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/video-pipeline && test ! -f frontend/src/components/ManifestLibrary.tsx && test ! -f frontend/src/components/ManifestCreator.tsx && test ! -f frontend/src/components/ManifestCard.tsx && test ! -f frontend/src/components/ManifestSelector.tsx && echo "Files deleted" && cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
    <manual>Verify no build errors after deletion</manual>
  </verify>
  <done>Four orphan Manifest*.tsx files deleted, frontend builds cleanly, no dangling imports</done>
</task>

</tasks>

<verification>
1. `grep -ri "manifest" frontend/src/components/ShotCard.tsx frontend/src/components/EditForkPanel.tsx | grep -v "manifest_tag\|manifest_adherence\|manifest_id\|manifestId"` — zero user-facing "manifest" strings remain (only API field names)
2. `ls frontend/src/components/Manifest*.tsx 2>/dev/null` — returns nothing (files deleted)
3. `grep -r "ManifestLibrary\|ManifestCreator\|ManifestCard\|ManifestSelector" frontend/src/ --include="*.tsx" --include="*.ts"` — zero results
4. `cd frontend && npx tsc --noEmit` — clean TypeScript build
5. `cd frontend && npm run lint` — no lint errors
</verification>

<success_criteria>
- ShotCard.tsx uses "Production Bible" and "Bible" in user-facing strings
- EditForkPanel.tsx imports fetchProductionBibleAssets and uses "Production Bible" in strings
- ManifestLibrary.tsx, ManifestCreator.tsx, ManifestCard.tsx, ManifestSelector.tsx deleted
- Frontend builds without TypeScript errors
- No dangling imports of deleted components
</success_criteria>

<output>
After completion, create `.planning/phases/19-bible-context-fix-cleanup/19-02-SUMMARY.md`
</output>
