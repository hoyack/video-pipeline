---
phase: 28-voice-script-tts-lip-sync-layer
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - backend/vidpipe/db/models.py
  - backend/vidpipe/db/__init__.py
  - backend/vidpipe/services/voice_mixer.py
  - backend/vidpipe/services/lip_sync/base.py
  - backend/vidpipe/services/lip_sync/registry.py
  - backend/vidpipe/services/lip_sync/fake_adapter.py
  - backend/vidpipe/services/voice_script_service.py
  - backend/vidpipe/api/voice_script.py
  - backend/vidpipe/pipeline/stitcher.py
  - frontend/src/api/types.ts
  - frontend/src/api/client.ts
  - frontend/src/components/VoiceScriptTab.tsx
  - backend/tests/test_voice_mixer.py
  - backend/tests/test_lip_sync_jobs.py
autonomous: true
requirements:
  - VOICE-06
  - VOICE-07
  - VOICE-08

must_haves:
  truths:
    - "Generated voice lines can be composed into persisted mix artifacts"
    - "Voice mix overlay writes a new video artifact and never overwrites original clips"
    - "LipSyncJob rows track queued/running/ready/failed/skipped post-video work"
    - "Lip-sync adapter interface supports fake/test adapter and future Wav2Lip/MuseTalk/LatentSync adapters"
    - "Eligibility checks skip unsupported shots with clear reasons"
    - "UI exposes mix and lip-sync status plus skip/failure warnings"
  artifacts:
    - path: "backend/vidpipe/services/voice_mixer.py"
      provides: "ffprobe duration, ffmpeg line mixing, and non-destructive video overlay helpers"
      exports: ["VoiceMixer"]
    - path: "backend/vidpipe/services/lip_sync/base.py"
      provides: "LipSyncAdapter interface and result type"
      exports: ["LipSyncAdapter", "LipSyncResult"]
    - path: "backend/vidpipe/services/lip_sync/fake_adapter.py"
      provides: "Deterministic lip-sync adapter for tests"
      exports: ["FakeLipSyncAdapter"]
  key_links:
    - from: "backend/vidpipe/api/voice_script.py"
      to: "backend/vidpipe/services/voice_mixer.py"
      via: "mix endpoint calls VoiceMixer"
      pattern: "VoiceMixer"
    - from: "backend/vidpipe/api/voice_script.py"
      to: "backend/vidpipe/services/lip_sync/registry.py"
      via: "lip-sync endpoint resolves adapter"
      pattern: "get_lip_sync_adapter"
    - from: "backend/vidpipe/pipeline/stitcher.py"
      to: "lip_sync_jobs.output_clip_path"
      via: "prefer ready lip-synced clip when present"
      pattern: "lip_sync"
---

<objective>
Add deterministic voice mixing and optional lip-sync job infrastructure. Generated voice lines can be mixed into shot/scene audio artifacts, overlaid onto videos without replacing originals, and used to queue lip-sync work through a provider adapter boundary.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/voice-plan.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-voice-script-tts-lip-sync-layer/28-CONTEXT.md
@.planning/phases/28-voice-script-tts-lip-sync-layer/28-RESEARCH.md
@.planning/phases/28-voice-script-tts-lip-sync-layer/28-01-PLAN.md

@backend/vidpipe/db/models.py
@backend/vidpipe/api/voice_script.py
@backend/vidpipe/services/voice_script_service.py
@backend/vidpipe/pipeline/stitcher.py
@backend/vidpipe/services/file_manager.py
@frontend/src/components/VoiceScriptTab.tsx
@frontend/src/api/types.ts
@frontend/src/api/client.ts

<interfaces>
Plan 01 creates:

```python
class VoiceLine(Base):
    audio_path: Mapped[Optional[str]]
    duration_seconds: Mapped[Optional[float]]
    start_time_seconds: Mapped[Optional[float]]
    generation_status: Mapped[str]
    lip_sync_mode: Mapped[str]
```

Target adapter interface:

```python
class LipSyncAdapter(ABC):
    async def sync_clip(
        self,
        input_video_path: Path,
        input_audio_path: Path,
        output_video_path: Path,
        *,
        face_hint: dict | None = None,
        options: dict | None = None,
    ) -> LipSyncResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mix artifact and lip-sync job persistence</name>
  <files>
    backend/vidpipe/db/models.py
    backend/vidpipe/db/__init__.py
  </files>
  <action>
Add `VoiceMixArtifact` ORM model:

- `id`, `voice_script_id`, `scene_id`, `shot_id`
- `artifact_type`: `SHOT_VOICE_STEM`, `SCENE_VOICE_STEM`, `SHOT_VIDEO_OVERLAY`, `SCENE_VIDEO_OVERLAY`
- `audio_path`, `video_path`, `duration_seconds`, `status`, `error_message`
- `created_at`

Add `LipSyncJob` ORM model:

- `id`, `voice_line_id`, `shot_id`, `input_clip_id`
- `input_audio_path`, `output_clip_path`
- `adapter_type`, `status`, `eligibility_reason`, `metrics_json`, `error_message`
- `created_at`, `completed_at`

Update startup migration path to create new tables.
  </action>
  <verify>
    <automated>pytest backend/tests/test_lip_sync_jobs.py::test_lip_sync_job_table_created -q</automated>
  </verify>
  <done>
    - mix artifacts persist audio/video outputs
    - lip-sync jobs persist queue and skip/failure state
    - migrations are non-destructive
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement VoiceMixer ffmpeg helpers</name>
  <files>
    backend/vidpipe/services/voice_mixer.py
    backend/vidpipe/services/voice_script_service.py
  </files>
  <action>
Create `VoiceMixer` with async public methods:

- `probe_duration(path: Path) -> float`
- `build_shot_voice_stem(session, voice_script_id, shot_id) -> VoiceMixArtifact`
- `build_scene_voice_stem(session, voice_script_id, scene_id) -> VoiceMixArtifact`
- `overlay_voice_on_video(input_video, voice_stem, output_video) -> Path`

Use `asyncio.to_thread` for subprocess work.

ffmpeg behavior:
- normalize line audio to a consistent sample rate
- apply per-line delay with `adelay`
- combine with `amix`
- use limiter/normalizer when reasonable
- write output under `tmp/productions/{production_id}/voice/mixes/`
- never overwrite original clip paths

Integrate service methods into `VoiceScriptService` where useful.
  </action>
  <verify>
    <automated>pytest backend/tests/test_voice_mixer.py -q</automated>
  </verify>
  <done>
    - fixture line audio files can be mixed into a stem
    - duration is measured and persisted
    - overlay writes a new video path
    - ffmpeg errors are captured in artifact status/error fields
  </done>
</task>

<task type="auto">
  <name>Task 3: Add lip-sync adapter boundary and eligibility</name>
  <files>
    backend/vidpipe/services/lip_sync/base.py
    backend/vidpipe/services/lip_sync/registry.py
    backend/vidpipe/services/lip_sync/fake_adapter.py
    backend/vidpipe/services/voice_script_service.py
  </files>
  <action>
Create lip-sync service package:

- `base.py`: `LipSyncAdapter`, `LipSyncResult`
- `registry.py`: `get_lip_sync_adapter(adapter_type)`
- `fake_adapter.py`: deterministic adapter for tests that copies input video or writes a known fixture output

Add eligibility logic to `VoiceScriptService`:
- skip non-dialogue lines
- skip `lip_sync_mode=NONE` or `SKIP`
- skip unresolved cast binding
- skip non-human `CastBinding.identity_type` by default
- skip if target shot or clip is missing
- skip if multiple dialogue speakers target the same shot in v1
- record skip reason in `LipSyncJob.eligibility_reason`

Queue ready jobs with status `QUEUED`. Running the fake adapter should update jobs to `READY` and set `output_clip_path`.
  </action>
  <verify>
    <automated>pytest backend/tests/test_lip_sync_jobs.py -q</automated>
  </verify>
  <done>
    - unsupported lines create skipped jobs with explicit reasons
    - eligible single-speaker lines create queued jobs
    - fake adapter produces ready jobs in tests
  </done>
</task>

<task type="auto">
  <name>Task 4: Add mix and lip-sync API/UI status</name>
  <files>
    backend/vidpipe/api/voice_script.py
    backend/vidpipe/pipeline/stitcher.py
    frontend/src/api/types.ts
    frontend/src/api/client.ts
    frontend/src/components/VoiceScriptTab.tsx
  </files>
  <action>
Add API endpoints:

- `POST /api/voice-scripts/{voice_script_id}/mix`
- `POST /api/voice-scripts/{voice_script_id}/lip-sync`
- `GET /api/voice-scripts/{voice_script_id}/jobs`

Update responses to include mix artifacts and lip-sync jobs/status.

Update `stitcher.py` to prefer a ready lip-synced replacement clip for a shot when present. Preserve fallback to original `VideoClip.local_path`.

Update frontend:
- Add Mix Voice Layer action.
- Add Lip Sync Eligible Shots action.
- Show aggregate mix/lip-sync status.
- Show per-line or per-shot status: ready, skipped, failed, queued.
- Show skip/failure reasons in the warnings panel.
  </action>
  <verify>
    <automated>pytest backend/tests/test_voice_script_api.py backend/tests/test_lip_sync_jobs.py -q</automated>
    <automated>cd frontend && npm run lint</automated>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>
    - UI can trigger mix and lip-sync operations
    - job status polling displays meaningful state
    - stitcher uses replacement clips only when ready and never loses originals
  </done>
</task>

</tasks>

<verification>
Run:

- `pytest backend/tests/test_voice_mixer.py -q`
- `pytest backend/tests/test_lip_sync_jobs.py -q`
- `pytest backend/tests/test_voice_script_api.py -q`
- `ruff check backend/vidpipe/services/voice_mixer.py backend/vidpipe/services/lip_sync backend/vidpipe/api/voice_script.py`
- `cd frontend && npm run lint`
- `cd frontend && npm run build`
</verification>

<success_criteria>
- VOICE-06 through VOICE-08 are implemented.
- Voice mix artifacts are persisted and playable/exportable.
- Lip-sync jobs are modeled, eligible/skipped deterministically, and adapter-backed.
- Original generated clips remain available after mix or lip-sync operations.
</success_criteria>
