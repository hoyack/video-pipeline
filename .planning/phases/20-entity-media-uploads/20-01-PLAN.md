---
phase: 20-entity-media-uploads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/vidpipe/api/characters.py
  - backend/vidpipe/api/sets_props.py
  - backend/vidpipe/api/sound.py
autonomous: true
requirements: [PBEX-01, PBEX-02, PBEX-07, PBEX-08, PBEX-16, PBEX-17]

must_haves:
  truths:
    - "POST /api/characters/:id/actor-refs accepts image upload and appends to actor_refs JSON array"
    - "POST /api/characters/:id/generate-appearance returns 422 if no actor_refs exist, otherwise generates base appearance text from character description"
    - "POST /api/wardrobes/:id/upload-reference accepts image upload and appends to reference_images JSON array"
    - "POST /api/generate-reverse-prompt accepts image upload and returns reverse_prompt text"
    - "POST /api/sonic-identities/:id/upload-audio accepts audio upload and stores reference_audio path"
    - "POST /api/score-themes/:id/upload-audio accepts audio upload and stores reference_audio path"
    - "POST /api/sfx/:id/upload-audio accepts audio upload and stores source_audio path"
  artifacts:
    - path: "backend/vidpipe/api/characters.py"
      provides: "Actor ref upload, generate appearance, wardrobe ref upload endpoints"
      contains: "upload_actor_ref"
    - path: "backend/vidpipe/api/sets_props.py"
      provides: "Standalone generate-reverse-prompt endpoint"
      contains: "generate_reverse_prompt"
    - path: "backend/vidpipe/api/sound.py"
      provides: "Audio upload endpoints for ScoreTheme, SFXItem, SonicIdentity"
      contains: "upload_score_theme_audio"
  key_links:
    - from: "backend/vidpipe/api/characters.py"
      to: "vidpipe.services.storage_backend"
      via: "get_storage_backend() for dual local/S3 storage"
      pattern: "get_storage_backend"
    - from: "backend/vidpipe/api/sets_props.py"
      to: "vidpipe.services.reverse_prompt_service"
      via: "ReversePromptService.reverse_prompt_asset()"
      pattern: "ReversePromptService"
    - from: "backend/vidpipe/api/sound.py"
      to: "vidpipe.services.storage_backend"
      via: "get_storage_backend() for audio file storage"
      pattern: "get_storage_backend"
---

<objective>
Add all missing file upload endpoints across Production Bible entity types.

Purpose: Phase 17 built entity CRUD but deferred media upload capabilities. This plan adds 7 upload endpoints following the established pattern from `upload_set_reference` and `upload_prop_reference` in sets_props.py -- same content-type validation, 10MB (images) / 20MB (audio) size limits, dual local/S3 storage, and graceful LLM degradation.

Output: 7 new API endpoints across 3 route files, all tested via curl.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-entity-media-uploads/20-RESEARCH.md
@backend/vidpipe/api/sets_props.py (reference pattern for upload endpoints)
@backend/vidpipe/api/characters.py (modify: add upload endpoints)
@backend/vidpipe/api/sound.py (modify: add audio upload endpoints)
@backend/vidpipe/services/storage_backend.py (storage abstraction)
@backend/vidpipe/services/reverse_prompt_service.py (for generate-reverse-prompt)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Character and wardrobe upload endpoints + standalone reverse-prompt</name>
  <files>
    backend/vidpipe/api/characters.py
    backend/vidpipe/api/sets_props.py
  </files>
  <action>
Add three endpoints to characters.py and one to sets_props.py. Add required imports at top of characters.py: `import asyncio`, `from fastapi import File, UploadFile`, `from vidpipe.services.storage_backend import get_storage_backend, LocalStorageBackend`.

**1. POST /api/characters/{character_id}/actor-refs** in characters.py:
- Validate content_type in ("image/png", "image/jpeg", "image/webp"), read content, validate <= 10MB
- Get Character from DB (404 if missing)
- Store via dual storage pattern (same as `upload_set_reference` in sets_props.py):
  - Local: write to `tmp/manifests/{bible_id}/characters/{char_id}/actor_refs/{filename}`
  - S3: key = `manifests/{bible_id}/characters/{char_id}/actor_refs/{filename}`, also write local copy
- CRITICAL: Append to actor_refs using NEW list to trigger SQLAlchemy change detection: `char.actor_refs = (char.actor_refs or []) + [stored_path]` (NOT `char.actor_refs.append()` which mutates in-place and SQLAlchemy won't detect)
- Commit, refresh, return full character dict via `_character_to_dict` (fetch wardrobes + voice profile for response)

**2. POST /api/characters/{character_id}/generate-appearance** in characters.py:
- Guard: return 422 if `char.actor_refs` is empty/None with message "Upload actor reference images first"
- Use `ReversePromptService.reverse_prompt_asset()` on the FIRST actor ref image to generate a base appearance description
- Import ReversePromptService inside the handler (lazy import, same pattern as sets_props.py)
- Call `svc.reverse_prompt_asset(first_ref_path, "CHARACTER", user_name=char.name)`
- Set `char.base_appearance = result["reverse_prompt"]`
- Wrap in try/except; on failure return 500 with detail message (don't silently swallow -- user initiated this explicitly)
- Commit, refresh, return full character dict
- Note: If the first ref is an S3 key, resolve to local path via `settings.storage.tmp_dir / key` (local copy should exist from actor-ref upload)

**3. POST /api/wardrobes/{wardrobe_id}/upload-reference** in characters.py:
- Same image validation pattern (png/jpeg/webp, 10MB max)
- Get Wardrobe from DB (404 if missing). Also fetch the character to get `production_bible_id` for storage path.
- Store at `tmp/manifests/{bible_id}/characters/{char_id}/wardrobes/{wardrobe_id}/{filename}` (local) or equivalent S3 key
- Append to reference_images using NEW list: `w.reference_images = (w.reference_images or []) + [stored_path]`
- Commit, refresh, return wardrobe dict via `_wardrobe_to_dict`

**4. POST /api/generate-reverse-prompt** in sets_props.py:
- Standalone endpoint (not entity-specific) accepting UploadFile
- Validate image content type and 10MB size limit
- Save to a temp location: `tmp/reverse_prompt_uploads/{uuid4()}/{filename}`
- Call `ReversePromptService().reverse_prompt_asset(path, "OBJECT")` (generic type)
- Return `{"reverse_prompt": result["reverse_prompt"], "visual_description": result.get("visual_description", "")}`
- Wrap in try/except; on LLM failure return 500 with detail
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/video-pipeline && python -c "from vidpipe.api.characters import upload_actor_ref, generate_appearance, upload_wardrobe_reference; from vidpipe.api.sets_props import generate_reverse_prompt; print('All 4 endpoints importable')"</automated>
    <manual>Verify endpoints accept UploadFile parameter and follow dual storage pattern</manual>
  </verify>
  <done>4 image upload endpoints exist and are importable: actor-ref upload appends to JSON array with new-list pattern, generate-appearance uses ReversePromptService with 422 guard, wardrobe ref upload appends to reference_images, standalone generate-reverse-prompt returns prompt text</done>
</task>

<task type="auto">
  <name>Task 2: Audio upload endpoints for Sound Department entities</name>
  <files>
    backend/vidpipe/api/sound.py
  </files>
  <action>
Add three audio upload endpoints to sound.py. Add required imports at top: `import asyncio`, `from fastapi import File, UploadFile`, `from vidpipe.services.storage_backend import get_storage_backend, LocalStorageBackend`.

Define audio MIME type constant at module level:
```python
ALLOWED_AUDIO_TYPES = ("audio/mpeg", "audio/wav", "audio/ogg", "audio/webm", "audio/mp4", "audio/x-m4a")
```

**1. POST /api/score-themes/{score_theme_id}/upload-audio**:
- Validate content_type in ALLOWED_AUDIO_TYPES, read content, validate <= 20MB (audio files are larger than images)
- Get ScoreTheme from DB (404 if missing)
- Store via dual storage pattern:
  - Local: write to `tmp/manifests/{bible_id}/score_themes/{theme_id}/{filename}`
  - S3: key = `manifests/{bible_id}/score_themes/{theme_id}/{filename}`, also write local copy
- Set `theme.reference_audio = stored_path`
- Commit, refresh, return score theme dict (copy the inline dict construction used in existing endpoints like `get_score_theme`)

**2. POST /api/sfx/{sfx_item_id}/upload-audio**:
- Same audio validation pattern (ALLOWED_AUDIO_TYPES, 20MB max)
- Get SFXItem from DB (404 if missing)
- Store at `tmp/manifests/{bible_id}/sfx/{item_id}/{filename}` (local) or equivalent S3 key
- Set `item.source_audio = stored_path`
- Commit, refresh, return SFX item dict

**3. POST /api/sonic-identities/{sonic_identity_id}/upload-audio** (NOT under /sets/ to keep it entity-direct):
- Same audio validation pattern
- Get SonicIdentity from DB (404 if missing). Need to look up the parent Set to get `production_bible_id` for storage path: `set_obj = await session.get(Set, si.set_id)`
- Store at `tmp/manifests/{bible_id}/sets/{set_id}/sonic_identity/{filename}` (local) or equivalent S3 key
- Set `si.reference_audio = stored_path`
- Commit, refresh, return sonic identity dict via `_sonic_identity_to_dict` (import from sets_props.py is unnecessary -- just inline the dict or import SonicIdentity and construct inline)
- Actually, since `_sonic_identity_to_dict` is in sets_props.py, define a local helper or return the dict inline matching the same shape.

Note: Add `from vidpipe.db.models import Set, SonicIdentity` to the imports in sound.py (Set and SonicIdentity are currently only imported in sets_props.py).
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/video-pipeline && python -c "from vidpipe.api.sound import upload_score_theme_audio, upload_sfx_audio, upload_sonic_identity_audio; print('All 3 audio endpoints importable')"</automated>
    <manual>Verify endpoints validate audio MIME types and use 20MB size limit</manual>
  </verify>
  <done>3 audio upload endpoints exist: ScoreTheme reference_audio, SFXItem source_audio, SonicIdentity reference_audio -- all validate audio MIME types, use 20MB limit, follow dual storage pattern, and return updated entity dicts</done>
</task>

</tasks>

<verification>
All 7 upload endpoints are importable and follow consistent patterns:
- Image endpoints: validate png/jpeg/webp, 10MB limit
- Audio endpoints: validate mpeg/wav/ogg/webm/mp4/x-m4a, 20MB limit
- All use get_storage_backend() with conditional local/S3 storage
- All use asyncio.to_thread() for sync file writes
- JSON array fields (actor_refs, reference_images) use new-list pattern for SQLAlchemy change detection

```bash
cd /home/ubuntu/work/video-pipeline && python -c "
from vidpipe.api.characters import upload_actor_ref, generate_appearance, upload_wardrobe_reference
from vidpipe.api.sets_props import generate_reverse_prompt
from vidpipe.api.sound import upload_score_theme_audio, upload_sfx_audio, upload_sonic_identity_audio
print('All 7 endpoints importable')
"
```
</verification>

<success_criteria>
7 new upload endpoints exist across 3 route files, all importable without errors, following the established dual-storage pattern from upload_set_reference.
</success_criteria>

<output>
After completion, create `.planning/phases/20-entity-media-uploads/20-01-SUMMARY.md`
</output>
