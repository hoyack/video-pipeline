---
phase: 17-production-bible-entity-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/vidpipe/db/models.py
  - backend/vidpipe/db/__init__.py
autonomous: true
requirements: [PBEX-01, PBEX-02, PBEX-03, PBEX-07, PBEX-08, PBEX-13, PBEX-16, PBEX-17, PBEX-20]

must_haves:
  truths:
    - "Character, Wardrobe, VoiceProfile, Set, SonicIdentity, Prop, ScoreTheme, SFXItem tables exist after init_database()"
    - "Scene.score_theme_id nullable FK column exists referencing score_themes(id)"
    - "VoiceProfile and SonicIdentity enforce 1:1 via unique constraint on parent FK"
    - "All new tables have production_bible_id FK indexed for efficient per-bible queries"
  artifacts:
    - path: "backend/vidpipe/db/models.py"
      provides: "8 new ORM models: Character, Wardrobe, VoiceProfile, Set, SonicIdentity, Prop, ScoreTheme, SFXItem"
      contains: "class Character"
    - path: "backend/vidpipe/db/__init__.py"
      provides: "Model imports and score_theme_id migration entry"
      contains: "score_theme_id"
  key_links:
    - from: "backend/vidpipe/db/models.py"
      to: "backend/vidpipe/db/__init__.py"
      via: "model imports for create_all() discovery"
      pattern: "from vidpipe\\.db\\.models import.*Character"
---

<objective>
Add all 8 new ORM models (Character, Wardrobe, VoiceProfile, Set, SonicIdentity, Prop, ScoreTheme, SFXItem) to the database layer, plus the Scene.score_theme_id FK migration entry.

Purpose: Establish the data layer that all subsequent API and UI plans depend on. New tables are auto-created by `Base.metadata.create_all()` once models exist in models.py.
Output: 8 new ORM model classes and 1 new ALTER TABLE migration for score_theme_id.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-production-bible-entity-expansion/17-RESEARCH.md
@backend/vidpipe/db/models.py
@backend/vidpipe/db/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 8 ORM entity models to models.py</name>
  <files>backend/vidpipe/db/models.py</files>
  <action>
Add 8 new ORM model classes to `backend/vidpipe/db/models.py`, placed AFTER the `Sequence` class (since they reference `production_bibles` via FK, which is already defined above).

**Character model** (`characters` table):
- `id`: UUID PK with default=uuid.uuid4
- `production_bible_id`: UUID FK to production_bibles.id, indexed
- `name`: Text, required
- `role`: String(30) — values: PROTAGONIST, ANTAGONIST, SUPPORTING, EXTRA
- `description`: Optional Text
- `arc`: Optional Text
- `actor_refs`: Optional JSON (list of image URLs/keys)
- `base_appearance`: Optional Text
- `prompt_tags`: Optional JSON (list of strings)
- `created_at`: datetime with server_default=func.now()
- `updated_at`: datetime with server_default=func.now(), onupdate=func.now()

**Wardrobe model** (`wardrobes` table):
- `id`: UUID PK
- `character_id`: UUID FK to characters.id, indexed
- `label`: Text, required
- `reference_images`: Optional JSON (list of image keys)
- `scene_context`: Optional Text
- `prompt_descriptor`: Optional Text
- `is_default`: Boolean, default=False, server_default=text("false")
- `created_at`: datetime with server_default=func.now()

**VoiceProfile model** (`voice_profiles` table):
- `id`: UUID PK
- `character_id`: UUID FK to characters.id, unique=True, indexed (enforces 1:1)
- `voice_id`: Optional String(200)
- `adapter_type`: String(50), default="ELEVENLABS"
- `style_notes`: Optional Text
- `sample_audio`: Optional String(500)
- `created_at`: datetime with server_default=func.now()

**Set model** (`sets` table):
- `id`: UUID PK
- `production_bible_id`: UUID FK to production_bibles.id, indexed
- `name`: Text, required
- `reference_image`: Optional String(500)
- `reverse_prompt`: Optional Text
- `style_tags`: Optional JSON
- `lighting_notes`: Optional Text
- `prompt_tags`: Optional JSON
- `created_at`: datetime with server_default=func.now()
- `updated_at`: datetime with server_default=func.now(), onupdate=func.now()

**SonicIdentity model** (`sonic_identities` table):
- `id`: UUID PK
- `set_id`: UUID FK to sets.id, unique=True, indexed (enforces 1:1)
- `ambience_description`: Optional Text
- `reference_audio`: Optional String(500)
- `generation_prompt`: Optional Text
- `created_at`: datetime with server_default=func.now()

**Prop model** (`props` table):
- `id`: UUID PK
- `production_bible_id`: UUID FK to production_bibles.id, indexed
- `name`: Text, required
- `reference_image`: Optional String(500)
- `description`: Optional Text
- `associated_characters`: Optional JSON (list of character UUID strings)
- `prompt_tags`: Optional JSON
- `created_at`: datetime with server_default=func.now()
- `updated_at`: datetime with server_default=func.now(), onupdate=func.now()

**ScoreTheme model** (`score_themes` table):
- `id`: UUID PK
- `production_bible_id`: UUID FK to production_bibles.id, indexed
- `name`: Text, required
- `mood_descriptors`: Optional JSON (list of strings)
- `tempo_notes`: Optional Text
- `usage_notes`: Optional Text
- `reference_audio`: Optional String(500)
- `generation_prompt`: Optional Text
- `adapter_type`: String(50), default="MUSIC_GEN"
- `created_at`: datetime with server_default=func.now()
- `updated_at`: datetime with server_default=func.now(), onupdate=func.now()

**SFXItem model** (`sfx_items` table):
- `id`: UUID PK
- `production_bible_id`: UUID FK to production_bibles.id, indexed
- `name`: Text, required
- `category`: String(30) — values: IMPACT, MECHANICAL, NATURAL, UI, FOLEY, AMBIENCE
- `source_audio`: Optional String(500)
- `generation_prompt`: Optional Text
- `tags`: Optional JSON (list of strings)
- `created_at`: datetime with server_default=func.now()
- `updated_at`: datetime with server_default=func.now(), onupdate=func.now()

Follow the exact `Mapped[Type]` annotation pattern used by existing models. Use `server_default=text("false")` for boolean defaults (works on both SQLite and PostgreSQL). Do NOT use Python Enum types — use String columns with validation at the API layer.
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/video-pipeline && python -c "from vidpipe.db.models import Character, Wardrobe, VoiceProfile, Set, SonicIdentity, Prop, ScoreTheme, SFXItem; print('All 8 models import OK')"</automated>
  </verify>
  <done>8 new ORM model classes exist in models.py with correct column types, FKs, and constraints</done>
</task>

<task type="auto">
  <name>Task 2: Register model imports and add score_theme_id migration</name>
  <files>backend/vidpipe/db/__init__.py</files>
  <action>
In `backend/vidpipe/db/__init__.py`:

1. **Add model imports** to the existing import line from `vidpipe.db.models`. Add: `Character, Wardrobe, VoiceProfile, Set, SonicIdentity, Prop, ScoreTheme, SFXItem`. This ensures `Base.metadata.create_all()` discovers the new tables.

2. **Add migration entry** for `score_theme_id` on the `scenes` table. In `_run_migrations()`, append to the migrations list:
```python
# Phase 17: score_theme_id FK on scenes for Director agent compatibility
"ALTER TABLE scenes ADD COLUMN score_theme_id {uuid_type} REFERENCES score_themes(id)",
```

This uses the existing `{uuid_type}` placeholder pattern (TEXT for SQLite, UUID for PostgreSQL). The `score_themes` table is created by `create_all()` which runs before `_run_migrations()`, so the FK reference is valid.

Do NOT add CREATE TABLE statements for the new entity tables — `create_all()` handles that automatically from the ORM models.
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/video-pipeline && python -c "
from vidpipe.db.models import Character, Wardrobe, VoiceProfile, Set, SonicIdentity, Prop, ScoreTheme, SFXItem, Base
tables = Base.metadata.tables
expected = ['characters', 'wardrobes', 'voice_profiles', 'sets', 'sonic_identities', 'props', 'score_themes', 'sfx_items']
for t in expected:
    assert t in tables, f'Missing table: {t}'
print(f'All {len(expected)} tables registered in metadata')
# Check score_theme_id migration exists
import inspect
from vidpipe.db import _run_migrations
src = inspect.getsource(_run_migrations)
assert 'score_theme_id' in src, 'Missing score_theme_id migration'
print('score_theme_id migration entry present')
"</automated>
  </verify>
  <done>All 8 new entity tables are registered in SQLAlchemy metadata and will be auto-created at startup. Scene.score_theme_id migration entry exists for existing databases.</done>
</task>

</tasks>

<verification>
1. All 8 model classes import without errors
2. `Base.metadata.tables` contains all 8 new table names
3. `_run_migrations()` contains the score_theme_id ALTER TABLE entry
4. No regressions in existing model imports
</verification>

<success_criteria>
- 8 new ORM models defined with correct column types, FK constraints, and indices
- VoiceProfile.character_id and SonicIdentity.set_id have unique=True for 1:1 enforcement
- All production_bible_id FKs are indexed
- score_theme_id migration entry uses {uuid_type} placeholder
- Existing models and imports are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/17-production-bible-entity-expansion/17-01-SUMMARY.md`
</output>
