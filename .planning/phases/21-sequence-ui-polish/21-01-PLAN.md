---
phase: 21-sequence-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/vidpipe/api/sequences.py
  - frontend/src/api/types.ts
  - frontend/src/api/client.ts
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "PUT /api/sequences/{id}/scenes/reorder accepts an ordered list of scene IDs and updates scene_order for each"
    - "SceneListItem TypeScript type includes scene_order field"
    - "Frontend client has reorderScenesInSequence() function that calls the new endpoint"
  artifacts:
    - path: "backend/vidpipe/api/sequences.py"
      provides: "Bulk scene reorder within sequence endpoint"
      contains: "reorder_scenes_in_sequence"
    - path: "frontend/src/api/types.ts"
      provides: "scene_order field on SceneListItem, SceneReorderInSequenceRequest type"
      contains: "scene_order"
    - path: "frontend/src/api/client.ts"
      provides: "reorderScenesInSequence client function"
      contains: "reorderScenesInSequence"
  key_links:
    - from: "frontend/src/api/client.ts"
      to: "/api/sequences/{id}/scenes/reorder"
      via: "fetch PUT request"
      pattern: "sequences.*scenes.*reorder"
---

<objective>
Add the missing backend endpoint for bulk scene reorder within a sequence and fix the frontend TypeScript types and API client to support all four sequence UI polish features.

Purpose: Plan 02 (frontend UI wiring) depends on these backend and type foundations.
Output: One new backend endpoint, updated SceneListItem type, new client function.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-sequence-ui-polish/21-RESEARCH.md
@backend/vidpipe/api/sequences.py
@frontend/src/api/types.ts
@frontend/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bulk scene reorder endpoint to sequences.py</name>
  <files>backend/vidpipe/api/sequences.py</files>
  <action>
Add a new endpoint `PUT /api/sequences/{sequence_id}/scenes/reorder` to `sequences.py` that accepts a `SceneReorderRequest` body (a Pydantic model with `scene_ids: list[str]` — an ordered list of scene UUIDs).

Implementation:
1. Add `SceneReorderRequest` Pydantic model with `scene_ids: list[str]` field.
2. Create `reorder_scenes_in_sequence(sequence_id: str, body: SceneReorderRequest)` endpoint handler.
3. Validate the sequence exists (404 if not).
4. Query all Scene.id values where Scene.sequence_id == sequence_id to get existing scene IDs.
5. Validate every scene_id in the request body belongs to this sequence (422 if not).
6. For each (idx, scene_id) in the ordered list, execute `update(Scene).where(Scene.id == uuid.UUID(sid)).values(scene_order=idx)`.
7. Commit and return `{"status": "reordered"}`.

Follow the exact pattern of the existing `reorder_sequences` endpoint but operating on Scene rows within one sequence instead of Sequence rows within one production.
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/video-pipeline && python -c "
from vidpipe.api.sequences import sequence_router, SceneReorderRequest
routes = [r.path for r in sequence_router.routes]
assert '/sequences/{sequence_id}/scenes/reorder' in routes, f'Missing route. Found: {routes}'
print('Endpoint registered successfully')
print(f'SceneReorderRequest fields: {SceneReorderRequest.model_fields.keys()}')
"</automated>
  </verify>
  <done>PUT /api/sequences/{id}/scenes/reorder endpoint exists, validates scene ownership, updates scene_order for each scene in order</done>
</task>

<task type="auto">
  <name>Task 2: Add scene_order to SceneListItem type and reorderScenesInSequence client function</name>
  <files>frontend/src/api/types.ts, frontend/src/api/client.ts</files>
  <action>
In `frontend/src/api/types.ts`:
1. Add `scene_order?: number | null;` field to the `SceneListItem` interface (after `sequence_id`).
2. Add a new `SceneReorderInSequenceRequest` interface with `scene_ids: string[];` field, placed near the existing `SequenceReorderRequest`.

In `frontend/src/api/client.ts`:
1. Import `SceneReorderInSequenceRequest` from types.
2. Add `reorderScenesInSequence(sequenceId: string, body: SceneReorderInSequenceRequest): Promise<{ status: string }>` function that calls `PUT /api/sequences/${sequenceId}/scenes/reorder` with the body as JSON. Follow the exact pattern of the existing `reorderSequences` function.
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/video-pipeline/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>SceneListItem has scene_order field, SceneReorderInSequenceRequest type exists, reorderScenesInSequence client function exists, TypeScript compiles without errors</done>
</task>

</tasks>

<verification>
1. Backend endpoint registered: `python -c "from vidpipe.api.sequences import sequence_router; print([r.path for r in sequence_router.routes])"`
2. Frontend types compile: `cd frontend && npx tsc --noEmit`
3. New client function exported: `grep -n "reorderScenesInSequence" frontend/src/api/client.ts`
</verification>

<success_criteria>
- PUT /api/sequences/{id}/scenes/reorder endpoint exists and validates scene membership
- SceneListItem includes scene_order field (no more unsafe casts in SortableSequenceSection)
- reorderScenesInSequence() client function available for Plan 02 frontend work
- All existing tests and type checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-sequence-ui-polish/21-01-SUMMARY.md`
</output>
