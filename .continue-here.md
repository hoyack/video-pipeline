---
task: GitHub Issues #7 and #24 implementation
status: planned_not_started
last_updated: 2026-02-28T17:00:00Z
---

<current_state>
Planning complete for Phase 1 foundation issues. No code changes made yet. Full implementation plan approved and saved. 19 GitHub issues were created (#7-#25) in this session. Implementation has not started.
</current_state>

<completed_work>

- Created 19 GitHub issues (#7-#25) with full PRD bodies, labels, and blocked_by references
- Created all project labels (phase-1-foundation, phase-2-production-bible, etc.)
- Explored entire Manifest codebase (DB models, API routes, services, frontend components)
- Explored Scene/Production/Shot hierarchy and @dnd-kit patterns
- Designed and approved implementation plans for Issue #7 and #24
- Plan saved at: `/home/ubuntu/.claude/plans/linked-drifting-thacker.md`
</completed_work>

<remaining_work>

## Issue #7: Rename Manifest → Production Bible (do first)
1. Backend API surface rename in routes.py (Pydantic schemas, endpoint paths, JSON fields, handler names)
2. Add 301 redirects for old /api/manifests/* paths
3. Update manifesting_engine.py contact sheet URL (1 line)
4. Frontend: rename 4 component files (ManifestLibrary→ProductionBibleLibrary, etc.)
5. Frontend: update types.ts interfaces, client.ts functions/URLs
6. Frontend: update Layout.tsx nav, App.tsx routes/imports
7. Frontend: update all referencing components (GenerateForm, SceneDetail, EditModeOverlay, EditForkPanel, ShotCard)
8. Add department tab structure to ProductionBibleCreator.tsx (Casting, Art Department, Sound tabs)
9. Lint check and grep verification

## Issue #24: Introduce Sequence Grouping Layer (do second)
1. Add Sequence model to models.py + scene columns (sequence_id, scene_order)
2. Add migrations to db/__init__.py
3. Create backend/vidpipe/api/sequences.py with all CRUD endpoints
4. Register in app.py, extend SceneListItem and ProductionResponse
5. Frontend types + API client functions
6. Create 5+ new components (SequencedSceneList, SequenceHeader, SortableSceneCard, ColorPicker, SequenceContextMenu)
7. Implement @dnd-kit multi-container DnD
8. Lint check and verification
</remaining_work>

<decisions_made>

- **Surface-only rename for #7**: Only rename API paths, JSON fields, UI text, frontend routes/components. Keep internal Python class names, DB table/column names, S3 keys unchanged. Rationale: SQLite can't reliably rename tables, manifesting_engine.py is 51.6KB (too risky), S3 keys are persisted data.
- **ShotManifest/ShotAudioManifest NOT renamed**: Different domain concept (per-shot asset placement), not production bibles.
- **manifest_tag stays as-is**: Asset-level concept (CHAR_01, ENV_02), not a bible concept.
- **Sequence uses production_id not project_id**: Matches existing codebase convention.
- **duration_total computed not stored**: Avoids sync bugs, computed via SUM(Scene.total_duration).
- **Unsequenced = virtual grouping**: Scenes with sequence_id IS NULL, not a sentinel DB row.
- **New route file for sequences**: backend/vidpipe/api/sequences.py per CLAUDE.md convention.
- **Department tabs are frontend-only**: Filter on existing asset_type field client-side.
</decisions_made>

<blockers>
None — ready to implement.
</blockers>

<context>
GitHub issue number mapping (issues in this repo start at #7 due to pre-existing #3-#5):
- Old #4 → GH #7 (Production Bible rename)
- Old #5 → GH #8, Old #6 → GH #9, ..., Old #22 → GH #25
- Cross-references in issue bodies were remapped to new numbers.

The plan agent recommended doing #7 as a single PR (backend + frontend rename + department tabs) since there are no external API consumers. #24 should be a separate PR.

Key files for #7: routes.py (~6000 lines, 17+ manifest endpoints starting ~line 2918), manifest_service.py, manifesting_engine.py (only 1 line change), 4 frontend component files, types.ts, client.ts, Layout.tsx, App.tsx.

Key files for #24: models.py, db/__init__.py, new sequences.py, app.py, routes.py (extend SceneListItem/ProductionResponse), ProductionDetail.tsx, 5+ new components, EditModeOverlay.tsx (reference for DnD pattern).
</context>

<next_action>
Start with Issue #7 Task 1: Open routes.py and begin renaming Pydantic schemas and endpoint paths from manifest → production_bible. Read the plan at `/home/ubuntu/.claude/plans/linked-drifting-thacker.md` for full details.
</next_action>
